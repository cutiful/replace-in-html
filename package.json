{
  "name": "replace-in-html",
  "version": "1.0.2",
  "description": "Replace text in HTML strings without messing up element attributes.",
  "main": "dist/replace-in-html.js",
  "files": [
    "dist/*"
  ],
  "keywords": [
    "html",
    "replace",
    "find and replace"
  ],
  "homepage": "https://github.com/cutiful/replace-in-html#readme",
  "bugs": "https://github.com/cutiful/replace-in-html/issues",
  "repository": "github:cutiful/replace-in-html",
  "author": "Spring Raindrop",
  "license": "CDDL-1.0",
  "private": false,
  "browserslist": "> 0.25%, firefox >= 52, last 6 versions, not dead, not chrome 49, not ie > 0, not baidu > 0, not op_mini all",
  "scripts": {
    "build": "babel index.js -o dist/replace-in-html.js",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.6",
    "@babel/core": "^7.18.6",
    "@babel/preset-env": "^7.18.6",
    "babel-jest": "^28.1.2",
    "jest": "^28.1.2"
  },
  "dependencies": {
    "jest-environment-jsdom": "^28.1.2"
  }
}
